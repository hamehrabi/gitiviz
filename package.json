{
  "name": "gitiviz-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -b",
    "bundle": "node build/bundle.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "esbuild": "^0.23.0",
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.0"
  }
}
